---
import "@/styles/global.css";
import type { JSX } from "astro/jsx-runtime";

type Props = JSX.HtmlHTMLAttributes;

const { class: className = "", ...props } = Astro.props;
---

<div class={`w-full ${className}`} {...props}>
  <div class="container mx-auto py-4">
    <slot />
  </div>
</div>
